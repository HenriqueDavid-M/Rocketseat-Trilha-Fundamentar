const tipo = "c"

const temperatura = "32"

if (type = "c") {
    Number(temperatura)
    total = temperatura * 9/5 + 32
    console.log(`${temperatura}°C é igual a ${total}°F.`)
} else if (type = "f") {
    Number(temperatura)
    total = (temperatura - 32) * 5/9
    console.log(`${temperatura}°F é igual a ${total}°C.`)
}